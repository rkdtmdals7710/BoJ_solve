#include <iostream>
#include <vector>
using namespace std;

int main(){
    int T;
    int max_w, n;
    cin >> max_w >> n;
    vector<int> vec(n+1);
    while(n--){
        int tmp_i, tmp_w;
        cin >> tmp_i >> tmp_w;
        vec[tmp_i] = tmp_w;
    }

    while(!vec.empty()){
        
    }




    return 0;
}